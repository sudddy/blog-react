import { makeStyles } from "@material-ui/core/styles";

export const buttonStyles = makeStyles(theme => ({
  button: {
    color: "#fff",
    backgroundColor: "#45A292",
    marginTop: "40px"
  }
}));
